resilience4j:
  retry:
    instances:
      toB: { maxAttempts: 1, waitDuration: 0ms }
      toC: { maxAttempts: 1, waitDuration: 0ms }
  timelimiter:
    instances:
      toB: { timeoutDuration: 800ms }
      toC: { timeoutDuration: 5s }
