resilience4j:
  retry:
    instances:
      downstream:
        maxAttempts: 2
        waitDuration: 100ms
        enableExponentialBackoff: true
  timelimiter:
    instances:
      inner:
        timeoutDuration: 500ms
