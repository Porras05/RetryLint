resilience4j:
  retry:
    instances:
      downstream:
        maxAttempts: 3
        waitDuration: 50ms
  timelimiter:
    instances:
      incoming:
        timeoutDuration: 1300ms
      downstream:
        timeoutDuration: 400ms
